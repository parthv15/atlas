node_modules
.next
dist
.turbo
coverage

.env
.env.local
.env.*.local
!.env.example

*.log
.DS_Store

handover.md

.pnpm-store/

# GitHub App private keys. Broader than GitHub's own filename, because a
# renamed key is still a key.
*.pem